{
  "config": {
    "step": {
      "user": {
        "title": "Pick two+ country to monitor",
        "data": { "country": "Country+" }
      }
    },
    "abort": { "already_configured": "This country is already configured." }
  }
}
