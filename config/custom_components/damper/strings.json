{
  "config": {
    "step": {
      "user": {
        "title": "PUSH Button Addressing",
        "data": { "nextAddress": "Next Modbus address to be assigned" }
      }
    },
    "abort": { "already_configured": "This damper is already configured." }
  }
}
